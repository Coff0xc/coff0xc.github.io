{
  "name": "coff0xcblog",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run fetch-data && astro dev",
    "fetch-data": "node scripts/fetch-github-data.mjs && node scripts/fetch-skills-data.mjs",
    "build": "npm run fetch-data && astro check && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "@astrojs/sitemap": "^3.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.3"
  }
}
